<%= render 'shared/navbar' %>

<div class="container">
  <%= render "layouts/flash" %>

  <div class="list list-conversations" data-url=<%= list_account_conversations_url(@account) %> data-pusher-channel=<%= @account.slug %>></div>
  <%= render 'conversations/empty' %>
</div>

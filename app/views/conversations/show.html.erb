<script>
  _inbox = <%= Conversation.first(20).each_with_object({}) {|c, o| o[c.id] = ConversationSerializer.new(c, root: false) }.to_json.html_safe %>

  document.addEventListener('DOMContentLoaded', function(){
    React.renderComponent(App({inbox: _inbox}), document.body)
  })
</script>
